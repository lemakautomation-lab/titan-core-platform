# Scripts

Controlled automation scripts belong here.

Scripts must be deterministic, reviewable, safely scoped and free of embedded credentials. Destructive operations require explicit target validation.
